import { redirect } from "next/navigation"; import { RateList } from "@/features/rates/rate-list"; import { getSession } from "@/lib/auth/server"; import { can } from "@/lib/permissions"; export default async function RatesPage() { const session = await getSession(); const company = session?.activeCompany; if (!company) redirect("/select-company"); if (!can(company.role, "rates:view")) redirect("/dashboard"); return <RateList companyId={company.companyId} role={company.role} />; }
