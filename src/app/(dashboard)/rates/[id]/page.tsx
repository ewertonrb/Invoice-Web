import { notFound, redirect } from "next/navigation"; import { RateDetail } from "@/features/rates/rate-detail"; import { BackLink, PageHeader } from "@/features/rates/ui"; import { getSession } from "@/lib/auth/server"; import { can } from "@/lib/permissions"; export default async function RatePage({ params }: { params: Promise<{ id: string }> }) { const session = await getSession(); const company = session?.activeCompany; if (!company) redirect("/select-company"); if (!can(company.role, "rates:view")) redirect("/dashboard"); const id = Number((await params).id); if (!Number.isSafeInteger(id) || id <= 0) notFound(); return <div className="mx-auto max-w-6xl"><BackLink /><div className="mt-5"><PageHeader title="Rate table details" description="Review the effective period and configured items." /></div><RateDetail companyId={company.companyId} rateId={id} canManage={can(company.role, "rates:manage")} /></div>; }
