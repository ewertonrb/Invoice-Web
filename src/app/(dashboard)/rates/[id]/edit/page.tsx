import { notFound, redirect } from "next/navigation"; import { EditRate } from "@/features/rates/edit-rate"; import { BackLink, PageHeader } from "@/features/rates/ui"; import { getSession } from "@/lib/auth/server"; import { can } from "@/lib/permissions"; export default async function EditRatePage({ params }: { params: Promise<{ id: string }> }) { const session = await getSession(); const company = session?.activeCompany; if (!company) redirect("/select-company"); if (!can(company.role, "rates:manage")) redirect("/rates"); const id = Number((await params).id); if (!Number.isSafeInteger(id) || id <= 0) notFound(); return <div className="mx-auto max-w-6xl"><BackLink href={`/rates/${id}`} /><div className="mt-5"><PageHeader title="Edit rate table" description="Update this historical rate period carefully; backend overlap validation remains authoritative." /></div><EditRate companyId={company.companyId} rateId={id} /></div>; }
