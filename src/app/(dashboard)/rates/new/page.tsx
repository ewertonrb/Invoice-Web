import { redirect } from "next/navigation"; import { CreateRate } from "@/features/rates/create-rate"; import { BackLink, PageHeader } from "@/features/rates/ui"; import { getSession } from "@/lib/auth/server"; import { can } from "@/lib/permissions"; export default async function NewRatePage() { const session = await getSession(); const company = session?.activeCompany; if (!company) redirect("/select-company"); if (!can(company.role, "rates:manage")) redirect("/rates"); return <div className="mx-auto max-w-6xl"><BackLink /><div className="mt-5"><PageHeader title="Create rate table" description="Configure a historical rate period and its rate items." /></div><CreateRate companyId={company.companyId} /></div>; }
