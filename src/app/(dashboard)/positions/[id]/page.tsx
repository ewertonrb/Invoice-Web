import { notFound, redirect } from "next/navigation"; import { PositionDetail } from "@/features/positions/position-detail"; import { BackLink, PageHeader } from "@/features/positions/ui"; import { getSession } from "@/lib/auth/server"; import { can } from "@/lib/permissions";
export default async function PositionPage({ params }: { params: Promise<{ id: string }> }) { const session = await getSession(); const company = session?.activeCompany; if (!company) redirect("/select-company"); if (!can(company.role, "rates:manage")) redirect("/dashboard"); const id = Number((await params).id); if (!Number.isSafeInteger(id) || id <= 0) notFound(); return <div className="mx-auto max-w-5xl"><BackLink /><div className="mt-5"><PageHeader eyebrow="Project operations" title="Position details" description="Review the position, project and operational status." /></div><PositionDetail companyId={company.companyId} positionId={id} /></div>; }
