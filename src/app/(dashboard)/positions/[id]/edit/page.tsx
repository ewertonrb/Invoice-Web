import { notFound, redirect } from "next/navigation"; import { EditPosition } from "@/features/positions/edit-position"; import { BackLink, PageHeader } from "@/features/positions/ui"; import { getSession } from "@/lib/auth/server"; import { can } from "@/lib/permissions";
export default async function EditPositionPage({ params }: { params: Promise<{ id: string }> }) { const session = await getSession(); const company = session?.activeCompany; if (!company) redirect("/select-company"); if (!can(company.role, "rates:manage")) redirect("/dashboard"); const id = Number((await params).id); if (!Number.isSafeInteger(id) || id <= 0) notFound(); return <div className="mx-auto max-w-4xl"><BackLink href={`/positions/${id}`} /><div className="mt-5"><PageHeader eyebrow="Project operations" title="Edit position" description="Update the position name or assign it to another active project." /></div><EditPosition companyId={company.companyId} positionId={id} /></div>; }
