import { redirect } from "next/navigation"; import { CreatePosition } from "@/features/positions/create-position"; import { BackLink, PageHeader } from "@/features/positions/ui"; import { getSession } from "@/lib/auth/server"; import { can } from "@/lib/permissions";
export default async function NewPositionPage() { const session = await getSession(); const company = session?.activeCompany; if (!company) redirect("/select-company"); if (!can(company.role, "rates:manage")) redirect("/dashboard"); return <div className="mx-auto max-w-4xl"><BackLink /><div className="mt-5"><PageHeader eyebrow="Project operations" title="Create position" description="Add a position to an active project." /></div><CreatePosition companyId={company.companyId} /></div>; }
