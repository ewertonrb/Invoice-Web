import { notFound, redirect } from "next/navigation"; import { WorkLogDetail } from "@/features/work-logs/work-log-detail"; import { getSession } from "@/lib/auth/server"; import { can } from "@/lib/permissions"; export default async function Page({ params }: { params: Promise<{ id: string }> }) { const session = await getSession(); if (!session?.activeCompany) redirect("/select-company"); const id = Number((await params).id); if (!Number.isSafeInteger(id) || id <= 0) notFound(); return <div className="mx-auto max-w-5xl"><h1 className="text-3xl font-semibold">Work log details</h1><WorkLogDetail companyId={session.activeCompany.companyId} id={id} canReview={can(session.activeCompany.role, "workLogs:review")} canEdit={session.activeCompany.role === "WORKER"} /></div>; }
