import { redirect } from "next/navigation"; import { WorkLogForm } from "@/features/work-logs/work-log-form"; import { getSession } from "@/lib/auth/server"; import { can } from "@/lib/permissions"; export default async function Page({ params }: { params: Promise<{ id: string }> }) { const session = await getSession(); if (!session?.activeCompany) redirect("/select-company"); if (session.activeCompany.role !== "WORKER" || !can(session.activeCompany.role, "workLogs:own")) redirect("/work-logs"); const id = Number((await params).id); if (!Number.isSafeInteger(id) || id <= 0) redirect("/work-logs"); return <div className="mx-auto max-w-4xl"><h1 className="text-3xl font-semibold">Edit work log</h1><WorkLogForm companyId={session.activeCompany.companyId} id={id} /></div>; }
