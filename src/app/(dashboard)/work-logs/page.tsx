import { redirect } from "next/navigation"; import { WorkLogList } from "@/features/work-logs/work-log-list"; import { getSession } from "@/lib/auth/server"; import { can } from "@/lib/permissions"; export default async function Page() { const session = await getSession(); if (!session?.activeCompany) redirect("/select-company"); return <WorkLogList companyId={session.activeCompany.companyId} ownOnly={!can(session.activeCompany.role, "workLogs:review")} canCreate={can(session.activeCompany.role, "workLogs:own")} />; }
