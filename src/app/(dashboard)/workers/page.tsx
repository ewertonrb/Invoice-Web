import { redirect } from "next/navigation"; import { WorkerList } from "@/features/workers/worker-list"; import { getSession } from "@/lib/auth/server"; import { can } from "@/lib/permissions"; export default async function WorkersPage() { const session = await getSession(); const company = session?.activeCompany; if (!company) redirect("/select-company"); if (!can(company.role, "workers:manage")) redirect("/dashboard"); return <WorkerList companyId={company.companyId} />; }
