import { notFound, redirect } from "next/navigation"; import Link from "next/link"; import { WorkerDetail } from "@/features/workers/worker-detail"; import { getSession } from "@/lib/auth/server"; import { can } from "@/lib/permissions"; export default async function WorkerPage({ params }: { params: Promise<{ id: string }> }) { const session = await getSession(); const company = session?.activeCompany; if (!company) redirect("/select-company"); if (!can(company.role, "workers:manage")) redirect("/dashboard"); const id = Number((await params).id); if (!Number.isSafeInteger(id) || id <= 0) notFound(); return <div className="mx-auto max-w-5xl"><Link href="/workers" className="text-sm font-semibold text-emerald-700">← Back to workers</Link><h1 className="mt-5 text-3xl font-semibold">Worker profile</h1><WorkerDetail companyId={company.companyId} workerId={id} actorRole={company.role} /></div>; }
