import { redirect } from "next/navigation"; import { InvitationsPage } from "@/features/invitations/invitations-page"; import { getSession } from "@/lib/auth/server"; import { can } from "@/lib/permissions"; export default async function Page() { const session = await getSession(); const company = session?.activeCompany; if (!company) redirect("/select-company"); if (!can(company.role, "workers:manage")) redirect("/dashboard"); return <InvitationsPage companyId={company.companyId} />; }
