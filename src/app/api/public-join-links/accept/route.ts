import { clearToken, getToken } from "@/lib/auth/session"; import { forbiddenOrigin, isSameOrigin } from "@/lib/auth/request"; import { getApiUrl } from "@/lib/api/server"; const noStore = { "Cache-Control": "no-store" };
export async function POST(request: Request) { if (!isSameOrigin(request)) { const response = forbiddenOrigin(); response.headers.set("Cache-Control", "no-store"); return response; } const sessionToken = await getToken(); if (!sessionToken) return Response.json({ message: "Unauthenticated." }, { status: 401, headers: noStore }); let payload: unknown; try { payload = await request.json(); } catch { return Response.json({ message: "The request body must contain valid JSON." }, { status: 400, headers: noStore }); } const token = payload && typeof payload === "object" && "token" in payload ? (payload as { token?: unknown }).token : null; if (typeof token !== "string" || !token.trim()) return Response.json({ message: "Join token is required." }, { status: 400, headers: noStore }); try { const response = await fetch(getApiUrl("/public/join-links/accept"), { method: "POST", headers: { Accept: "application/json", "Content-Type": "application/json", Authorization: `Bearer ${sessionToken}` }, body: JSON.stringify({ token }), cache: "no-store" }); if (response.status === 401) await clearToken(); return new Response(response.body, { status: response.status, headers: { "content-type": response.headers.get("content-type") || "application/json", ...noStore } }); } catch { return Response.json({ message: "Could not connect to the server." }, { status: 503, headers: noStore }); } }
