"use client";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query"; import { queryKeys } from "@/lib/api/query-keys"; import { createRate, getPositions, getRate, getRates, setRateActive, updateRate } from "./api";
export function useRatePositions(companyId: number, activeOnly = false) { return useQuery({ queryKey: [...queryKeys.positions(companyId), { activeOnly }], queryFn: () => getPositions(activeOnly), enabled: companyId > 0 }); }
export function useRates(companyId: number, positionId?: number) { return useQuery({ queryKey: [...queryKeys.rates(companyId), { positionId: positionId ?? null }], queryFn: () => getRates(positionId!), enabled: companyId > 0 && Boolean(positionId) }); }
export function useRate(companyId: number, id: number) { return useQuery({ queryKey: queryKeys.rate(companyId, id), queryFn: () => getRate(id), enabled: companyId > 0 && id > 0 }); }
export function useCreateRate(companyId: number) { const client = useQueryClient(); return useMutation({ mutationFn: createRate, onSuccess: async () => client.invalidateQueries({ queryKey: queryKeys.rates(companyId) }) }); }
export function useUpdateRate(companyId: number, id: number) { const client = useQueryClient(); return useMutation({ mutationFn: (input: Parameters<typeof updateRate>[1]) => updateRate(id, input), onSuccess: async (data) => { client.setQueryData(queryKeys.rate(companyId, id), data); await client.invalidateQueries({ queryKey: queryKeys.rates(companyId) }); } }); }
export function useSetRateActive(companyId: number, id: number) { const client = useQueryClient(); return useMutation({ mutationFn: (active: boolean) => setRateActive(id, active), onSuccess: async (data, active) => { if (data) client.setQueryData(queryKeys.rate(companyId, id), data); else client.setQueryData(queryKeys.rate(companyId, id), (current: unknown) => current && typeof current === "object" ? { ...current, active } : current); await client.invalidateQueries({ queryKey: queryKeys.rates(companyId) }); } }); }
