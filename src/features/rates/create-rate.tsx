"use client"; import { useRouter } from "next/navigation"; import { useCreateRate } from "./hooks"; import { RateForm } from "./rate-form"; export function CreateRate({ companyId }: { companyId: number }) { const router = useRouter(); const mutation = useCreateRate(companyId); return <RateForm companyId={companyId} pending={mutation.isPending} serverError={mutation.error} submitLabel="Create rate table" onSubmit={(data) => mutation.mutate(data, { onSuccess: (rate) => router.push(`/rates/${rate.id}`) })} />; }
